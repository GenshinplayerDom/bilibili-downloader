# 1.3.0

## 下载体验

- 桌面端 DASH、直链视频和 M4A 下载支持暂停/继续。暂停中保留本次任务已完成的分片；退出应用后不保留续传状态。
- 失败和取消的任务可以一键重试。重试保留原视频、分 P 和下载设置，并重新获取地址。
- 桌面端自动为同名输出追加 `(1)`、`(2)`，通过独占创建避免并发覆盖。
- 任务使用独立的视频和设置快照；定时任务可以取消；清理完成任务保留暂停和等待中的任务。

## 修复

- 本地代理加入随机请求令牌、来源校验、目标域名白名单、超时和有限响应大小。
- Cookie 只发给指定的 B 站 API 域名，不发给 CDN、封面域名或短链目标。
- Windows 使用 Electron safeStorage，Android 使用 Android Keystore；迁移旧明文凭据，无可用加密服务时只保存在内存。
- 转义分 P / 剧集标题，加入 CSP、IPC 来源校验、导航限制和下载文件操作范围限制。
- 重写桌面下载核心，校验状态码、Content-Range、Content-Length 和实际字节数；等待写入关闭后提交文件。
- 分片失败后切换备用地址重试；支持网络取消、超时和全局连接数限制。
- 修复前端 WBI 密钥未重排的问题；油猴脚本动态刷新 WBI 密钥。
- 修复字幕接口数据读取、片段裁剪对独立音轨的处理、ffmpeg 进度参数和登录成功回调。
- Android 页面迁移到受控 HTTPS 资源域名，移除 file 页面通用访问权限，阻止外部页面导航。

## 构建与验证

- 更新并固定 Electron / 构建依赖，提交 npm 锁文件，修复 Windows 启动脚本。
- Windows 打包包含 FFmpeg、其许可证和来源说明。
- 补齐经过 SHA-256 校验的 Gradle 8.7 Wrapper；Android 发布签名从环境变量读取。
- 使用单一前端源文件，通过脚本同步 Android 资源并检查一致性。
- 新增 Node 核心回归测试、隔离账号/下载目录的 Electron 界面测试，以及 GitHub Actions。

## 明确限制

- 暂停/继续限桌面下载阶段；合并阶段支持取消，不支持暂停；MP3 浏览器转码不支持暂停。
- 跨重启续传、完整收藏夹分页、全局队列排序、Android DASH 合并尚未实现。
- 自动测试使用本地模拟媒体，不代表所有 B 站视频或账号权限均可下载。
