# 哔哩下载器 · Dom — 功能与优化规划（Roadmap）

> 本文档原为规划清单，**v1.2.0 已按清单全部实施**（18 项功能 + 15 项优化）。
> 当前版本：v1.2.0 · 实施范围说明：客户端全量支持；网页版 / Android 受平台限制的项目（如 MKV、片段裁剪、限速、播放器预览、设置文件导出）自动降级隐藏或提示。

---

## 一、可添加的新功能

### 🔴 高优先级

| # | 功能 | 说明 |
|---|------|------|
| 1 | **批量下载（合集 / 分P / 收藏夹）** | 一键下载整个视频合集、分 P 全部、UP 主投稿或收藏夹，按序生成任务队列 |
| 2 | **番剧全集下载** | 输入番剧链接后按集批量排队下载（当前需逐集选择） |
| 3 | **断点续传（跨启动）** | 下载中断后重启软件可继续（当前仅会话内取消，无跨启动续传） |
| 4 | **弹幕 / 字幕下载** | 可选下载 XML 弹幕、CC 字幕 / AI 字幕，与视频同目录保存 |
| 5 | **视频封面 / 视频信息导出** | 保存封面图、标题、UP 主、简介等元数据为 JSON / 文本 |

### 🟠 中优先级

| # | 功能 | 说明 |
|---|------|------|
| 6 | **下载队列管理与限速** | 全局暂停 / 恢复、队列顺序、单任务或全局限速（KB/s） |
| 7 | **代理多线路切换** | 内置代理支持多节点切换 / 测速，失败自动切换最优线路 |
| 8 | **视频合并格式扩展** | 除 MP4 外支持 MKV（保留多音轨 / 字幕轨），音频支持 FLAC 直存 |
| 9 | **定时下载** | 设置时间自动开始队列下载（避开高峰） |
| 10 | **收藏夹 / 稍后再看同步** | 登录后拉取收藏夹列表直接勾选批量下载 |
| 11 | **多语言界面** | 中 / 英 / 日界面切换，便于开源社区使用 |
| 12 | **设置云同步** | 登录后同步线程数、编码偏好、下载目录等设置（需自建同步服务或网盘） |

### 🟢 低优先级

| # | 功能 | 说明 |
|---|------|------|
| 13 | **播放器内置预览** | 下载完成后内置简易播放器预览视频 |
| 14 | **主题 / 深色模式** | 暗色主题、跟随系统外观 |
| 15 | **视频片段下载** | 输入起止时间下载视频片段（需转码引擎配合） |
| 16 | **手机扫码远程控制** | 手机网页端添加下载任务，电脑端自动执行 |
| 17 | **插件 / 油猴脚本版回归** | 重新发布浏览器脚本版，与客户端共用解析逻辑 |
| 18 | **视频 AI 总结** | 接入 AI 生成视频内容摘要（需第三方服务） |

---

## 二、可执行的优化事项

### 🔴 高优先级

| # | 优化 | 说明 |
|---|------|------|
| 1 | **DASH 分片调度优化** | 当前均分分片，改为动态任务池（完成的线程自动领取下一片），消除慢片尾效应 |
| 2 | **大文件内存占用** | 网页端 durl 下载走全内存 Blob，改为流式写入临时文件，降低大视频内存峰值 |
| 3 | **下载速度实时统计** | 任务卡片显示实时速度（MB/s）与剩余时间估算，便于用户感知 |
| 4 | **多任务并行上限控制** | 当前多任务无全局连接上限，增加总并发调度器，避免任务过多互相抢占带宽 |

### 🟠 中优先级

| # | 优化 | 说明 |
|---|------|------|
| 5 | **启动速度优化** | 延迟加载非首屏模块、懒初始化代理、启动画面，缩短冷启动时间 |
| 6 | **错误日志与反馈** | 统一错误日志（本地 log 文件），异常自动生成诊断包，便于排查 |
| 7 | **自动更新** | 接入自动更新检查（GitHub Releases / 自建源），一键更新到新版本 |
| 8 | **ffmpeg 合并进度细化** | 合并阶段显示实时进度（当前为固定进度条），大文件合并体验更好 |
| 9 | **UI 动画与反馈** | 下载按钮加载态、任务展开折叠动画、清晰度切换过渡 |
| 10 | **缓存策略优化** | DASH 缓存按清晰度 + 编码 + 登录态多维失效，支持手动锁定地址 |

### 🟢 低优先级

| # | 优化 | 说明 |
|---|------|------|
| 11 | **单元测试 / CI** | 为解析、签名、下载、合并等核心链路补充自动化测试与 CI 流水线 |
| 12 | **代码重构** | 前端 app.js 体积较大，拆分模块（解析 / 下载 / 设置 / 记录），降低维护成本 |
| 13 | **安全加固** | 注入防护、代理端口随机化、Cookie 加密存储（当前明文 JSON） |
| 14 | **多端一致** | Android 端支持 DASH 高清合并（需内置 ffmpeg 或纯 JS 合流方案） |
| 15 | **无障碍与国际化细节** | 键盘导航、ARIA 标签、帮助文档多语言 |

---

## 三、建议实施顺序（参考）

1. **v1.2.x**：下载速度实时统计 + 动态分片调度 + 多任务并发调度器（体验类高频项）
2. **v1.3.x**：断点续传 + 批量下载（合集 / 番剧 / 收藏夹）+ 下载队列与限速
3. **v1.4.x**：弹幕 / 字幕下载 + 封面元数据导出 + MKV 合流扩展
4. **v1.5.x**：自动更新 + 错误诊断包 + 设置云同步
5. **后续**：多语言 / 主题 / 定时下载 / 插件化等长尾功能

---

*规划由用户提出需求方向，仅供后续版本开发参考。*
