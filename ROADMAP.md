# 哔哩下载器 · GenshinplayerDom — 实施状态与后续规划

本表替代此前“全部实施”的笼统声明，以代码实际能力为准。

## v1.6.7 已完成
- 修复无法在线播放（根因：CSP `media-src` 仅允许 `'self' blob:`，外部视频流（代理流 / CDN 直链）被浏览器 URL 安全检查拒绝）。已放行 `http: https:`，实测播放器正常加载（readyState 4 / 可播放 / 时长完整）。
- 下载地址收集时 CDN 排序增强（主域优先，规避部分网络下 mcdn 域名 403）。

## v1.6.6 已完成
- 在线播放深度修复：代理 /stream 跟随 CDN 302 重定向（避免 <video> 跳到无 Referer 直链被防盗链 403）、Content-Type 兜底（m4s 强制 video/mp4）、主进程 webRequest 给 bilivideo/hdslb/acgvideo 请求自动注入 Referer + UA；播放失败自动降级链（1080P DASH → 1080P MP4 → 720P DASH → 720P MP4），并显示明确错误提示。
- 设置页「网络重置」：一键执行刷新 DNS、释放 / 重新获取 IP、重置 Winsock 与 IP 堆栈（管理员授权），用于修复被 B 站临时拉黑导致的清晰度降级 / 下载限速。
- 设置页「支持作者」：弹窗展示微信 / 支付宝收款码，支持切换。
- 使用教程补充网络被拉黑情景与在线播放说明。
- Android 端同步：收款码资源与上述功能随 assets 一并打包。

## v1.6.0 已完成
- 批量列表封面修复：合集 / 搜索 / 收藏夹封面统一 https（兼容 `//` 协议相对链接，避免 file:// 解析失败）。
- 下载任务悬浮窗：可拖动（标题栏），左上角 × 最小化为可拖动小球，点击小球展开；新任务自动展开。
- 下载队列页：左下入口（历史记录上方），全屏任务列表（实时状态 / 进度 / 可取消）+ 返回上一级。
- 左下主菜单：返回初始主界面（保留下载任务），位于下载队列上方。

## v1.5.0 已完成
- 软件内搜索：B 站视频搜索（WBI 签名 + 本机代理优先），结果直接进入下载列表。
- 跨重启续传：分片目录保留，重启后自动恢复未完成任务（Windows）。
- 自动更新：启动检查 + 设置页手动检查，发现新版本跳转 Release。
- 快捷键：Ctrl+D 触发下载、全局剪贴板粘贴识别链接。
- 主题跟随系统：浅色 / 深色 / 跟随系统三档。
- Android DASH 高清合并：原生 MediaMuxer 封装，无需 FFmpeg。

## v1.4.x 已完成

- **表格列表面板**（合集 / 收藏夹）：封面 / 标题 / 播放量 / 弹幕 / 时长展示，勾选 + 全选 / 全不选；单击「标题」进入单个视频详情，标题左侧区域点击判定为勾选该项；列表打开时同步全局清晰度 / 线程 / 编码 / 封装 / 音频格式选项。
- **并发下载调度**：设置「同时下载数上限」（默认 8，按设备内存给出建议，最高 16 线程）；超限任务显示「等待中（排队第 N 位）」，终态自动补位；一键中断 / 取消全部。
- **返回主菜单**：普通 / 批量页面右上角可返回初始页，本次会话下载历史保留。
- **简易模式恢复视频详情**（播放量 / 弹幕 / 点赞 / 评论 / 时长）。
- **任务卡悬浮**：列表 / 批量覆盖面板打开时，下载任务卡悬浮于底部可见，不遮挡列表操作。
- **批量面板完整选项**：番剧 / 分 P 批量保留清晰度、编码、封装、音频格式、线程数，并与主界面双向同步。

## v1.3.x 已完成

- 桌面下载暂停 / 继续、失败重试、同名文件保护。
- 分片范围 / 长度校验、备用 CDN 重试、取消和超时、全局连接池。
- 独立任务上下文，定时任务取消，保留暂停 / 等待中的任务。
- 代理请求鉴权与域名限制，桌面和 Android 登录凭据加密。
- 页面注入防护、IPC 来源检查、受管下载文件操作。
- WBI 签名修复、FFmpeg 打包、Gradle Wrapper、锁文件、资源同步和 CI。
- 合集（collectiondetail / ugc_season）批量下载，下载前按 bvid 动态补全 cid。
- 编码选择（H.264 / H.265 / AV1）、多线程并发下载（4–16 线程）、MP3 音质档位、下载历史页与一键清空、设置模块（代理 / 一键修复 / 多开 / 登录 / 教程 / 开源链接）。
- 本地核心回归测试与 Electron 界面验证。

## 部分支持

- 续传：仅本次会话暂停后复用已完成分片，不支持跨启动恢复。
- 批量下载：分 P / 剧集顺序执行；合集 / 收藏夹走表格列表勾选下载。
- 多端：Android 直链下载可用，DASH 高清合并仍缺失。
- 音频：M4A 原版直存、MP3 浏览器转码；不是所有音频格式均受支持。
- 队列：任务独立，可定时、取消；尚无全局队列排序和全局暂停按钮。
- 设置迁移：本地 JSON 导入导出，不是云同步。
- 信息导出：已有元数据和简介导出，不是 AI 总结。
- 国际化：保留原中英切换，未完成全界面翻译。

## 待实施

1. 跨重启恢复的任务数据库、稳定媒体身份与分片校验。
2. 收藏夹完整分页、失效视频过滤、队列排序与批量取消。
3. Android DASH 合流、可中断原生下载和设备端回归测试。
4. MP3 转码迁移到桌面原生管线以减少内存占用。
5. 自动更新、诊断包、前端模块拆分与无障碍改进。
6. 按明确需求另行设计云同步、远程控制和 AI 摘要。
